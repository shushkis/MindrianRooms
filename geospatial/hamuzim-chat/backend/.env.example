# Optional. If unset, /api/chat falls back to a rule-based mock reasoner
# that still runs a real search_parcels() filter -- just without an LLM.
# Free-tier friendly: get a key at https://aistudio.google.com/apikey
GEMINI_API_KEY=
